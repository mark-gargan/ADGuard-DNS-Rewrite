# AdGuard Home Configuration
ADGUARD_HOST=192.168.1.100
ADGUARD_PORT=80
ADGUARD_USERNAME=admin
ADGUARD_PASSWORD=your_password_here

# Hostname Configuration
# Option 1: Single hostname (legacy format)
HOSTNAME=myhost.local

# Option 2: Multiple hostnames (new format) - comma-separated
# HOSTNAMES=myhost.local,server.local,api.local,web.local

# Note: If both HOSTNAME and HOSTNAMES are set, HOSTNAMES takes precedence
